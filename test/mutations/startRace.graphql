mutation startRace {
  startRace {
    currentQuestion {
      title
      selectedOptions
      sentAt
      options
      index
    }
    currentSpeed
    currentServerTime
    startedAt
  }
}
