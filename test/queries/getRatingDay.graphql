query getRatingDay($day: Int!) {
  ratingDay(day: $day) {
    day
    userPosition
    users {
      bestAverageSpeed
      bestTime
      costumeColor
      name
      penaltyTime
      profileImageUrl
    }
    usersCount
  }
}
