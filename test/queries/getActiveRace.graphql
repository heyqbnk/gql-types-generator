query getActiveRace {
  activeRace {
    currentQuestion {
      title
      selectedOptions
      sentAt
      options
      index
    }
    currentSpeed
    currentServerTime
    startedAt
  }
}
